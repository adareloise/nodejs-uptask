extends layout 

include mixins/create-project

block contenido  
   .contenido-principal   
      h1= nombrePagina 
      
      if errores 
         each error in errores
            .alerta= error.texto
      
      +createProject(project)

      // pre= vardump(project)
